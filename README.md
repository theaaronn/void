# void

Bubble Tea v2 terminal app for dumping thoughts into the void.

## Controls

- `ctrl+f`: open confirmation modal
- `tab` / `left` / `right`: switch modal button
- `enter`: confirm selected modal action
- `esc`: close modal (or quit when modal is closed)
- `ctrl+c`: quit
